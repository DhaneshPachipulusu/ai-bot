apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aibot.fullname" . }}-config
  labels:
    {{- include "aibot.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
