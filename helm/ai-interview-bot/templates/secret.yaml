apiVersion: v1
kind: Secret
metadata:
  name: {{ include "aibot.fullname" . }}-secrets
  labels:
    {{- include "aibot.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.secrets }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
