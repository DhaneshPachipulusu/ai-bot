apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-interview-bot

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - backend-deployment.yaml
  - frontend-deployment.yaml
  - ingress.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/part-of: ai-interview-bot
  app.kubernetes.io/managed-by: kustomize
