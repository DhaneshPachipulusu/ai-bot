# NOTE: This is a TEMPLATE. Do not commit real secrets.
# In production, use Sealed Secrets, External Secrets Operator, or
# `kubectl create secret generic aibot-secrets --from-env-file=.env`
apiVersion: v1
kind: Secret
metadata:
  name: aibot-secrets
  namespace: ai-interview-bot
type: Opaque
stringData:
  GEMINI_API_KEY: "replace-me"
  OPENAI_API_KEY: ""
